node_modules/
dist/
.astro/
.claude/
*.mov

# Original full-size photos (keep only web/ and thumb/ optimized versions)
web_assets/photos/*/*.jpg

.vercel

# IndexNow private key (the matching <key>.txt in web_assets/ IS committed
# as the public ownership proof IndexNow requires).
.indexnow-key
